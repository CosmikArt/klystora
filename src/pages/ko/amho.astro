---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '암호 — Klystora';
const description = '암호를 핏고 숨겨진 명언을 밝히세요. Free daily puzzle in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">암호</h1>
      <p class="text-sand-500 mt-1">암호를 핏고 숨겨진 명언을 밝히세요</p>
    </div>
    
    <LazyGameWrapper game="cryptogram" lang="ko" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>