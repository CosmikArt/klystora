---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '아크로스틱 — Klystora';
const description = '단서를 풀어라. 각 답변의 첫 글자가 숨겨진 단어를 밝힌다!. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">아크로스틱</h1>
      <p class="text-sand-500 mt-1">단서를 풀어라. 각 답변의 첫 글자가 숨겨진 단어를 밝힌다!</p>
    </div>
    
    <LazyGameWrapper game="acrostic" lang="ko" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>