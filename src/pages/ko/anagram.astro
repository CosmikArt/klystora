---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '아나그램 — Klystora';
const description = '2분 안에 7글자로 단어 만들기. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">아나그램</h1>
      <p class="text-sand-500 mt-1">2분 안에 7글자로 단어 만들기</p>
    </div>
    
    <LazyGameWrapper game="anagrams" lang="ko" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>