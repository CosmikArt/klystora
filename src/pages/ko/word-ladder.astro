---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '워드래더 — Klystora';
const description = '한 글자씩 바꿔 목표 단어에 도달. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">워드래더</h1>
      <p class="text-sand-500 mt-1">한 글자씩 바꿔 목표 단어에 도달</p>
    </div>
    
    <LazyGameWrapper game="word-ladder" lang="ko" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>