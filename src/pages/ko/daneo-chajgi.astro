---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '단어찾기 — Klystora';
const description = '그리드에 숨겨진 모든 단어 찾기. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">단어찾기</h1>
      <p class="text-sand-500 mt-1">그리드에 숨겨진 모든 단어 찾기</p>
    </div>
    
    <LazyGameWrapper game="word-search" lang="ko" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>