---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '글자 상자 — Klystora';
const description = '다른 변에서 글자를 연결하여 단어 만들기. Free puzzle in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">글자 상자</h1>
      <p class="text-sand-500 mt-1">다른 변에서 글자를 연결하여 단어 만들기</p>
    </div>
    
    <LazyGameWrapper game="letter-boxed" lang="ko" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>