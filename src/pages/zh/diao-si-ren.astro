---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '吊死人 — Klystora';
const description = '逐个字母猜出隐藏的单词。你有6条命. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">吊死人</h1>
      <p class="text-sand-500 mt-1">逐个字母猜出隐藏的单词。你有6条命</p>
    </div>
    
    <LazyGameWrapper game="hangman" lang="zh" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>