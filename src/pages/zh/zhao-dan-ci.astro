---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '找单词 — Klystora';
const description = '在网格中找到所有隐藏的单词. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">找单词</h1>
      <p class="text-sand-500 mt-1">在网格中找到所有隐藏的单词</p>
    </div>
    
    <LazyGameWrapper game="word-search" lang="zh" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>