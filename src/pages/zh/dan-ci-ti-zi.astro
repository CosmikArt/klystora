---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '单词梯子 — Klystora';
const description = '每次改变一个字母到达目标词. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">单词梯子</h1>
      <p class="text-sand-500 mt-1">每次改变一个字母到达目标词</p>
    </div>
    
    <LazyGameWrapper game="word-ladder" lang="zh" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>