---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '藏头诗 — Klystora';
const description = '解答线索。每个答案的首字母揭示隐藏单词！. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">藏头诗</h1>
      <p class="text-sand-500 mt-1">解答线索。每个答案的首字母揭示隐藏单词！</p>
    </div>
    
    <LazyGameWrapper game="acrostic" lang="zh" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>