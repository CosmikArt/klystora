---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = 'Letter Boxed — Klystora';
const description = 'Connect letters from different sides to form words. Free puzzle in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">Letter Boxed</h1>
      <p class="text-sand-500 mt-1">Connect letters from different sides to form words. Use all 12 letters to win!</p>
    </div>
    
    <LazyGameWrapper game="letter-boxed" lang="ro" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>