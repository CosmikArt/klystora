---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = 'Kryptogramm — Klystora';
const description = 'Knacke die Chiffre und enthülle das versteckte Zitat. Free daily puzzle in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">Kryptogramm</h1>
      <p class="text-sand-500 mt-1">Knacke die Chiffre und enthülle das versteckte Zitat</p>
    </div>
    
    <LazyGameWrapper game="cryptogram" lang="de" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>