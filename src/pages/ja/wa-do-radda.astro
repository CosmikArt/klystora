---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = 'ワードラダー — Klystora';
const description = '1文字ずつ変更して目標の単語に到達. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">ワードラダー</h1>
      <p class="text-sand-500 mt-1">1文字ずつ変更して目標の単語に到達</p>
    </div>
    
    <LazyGameWrapper game="word-ladder" lang="ja" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>