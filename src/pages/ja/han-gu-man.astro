---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = 'ハングマン — Klystora';
const description = '一文字ずつ隠された単語を当ててください。6回のチャンス. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">ハングマン</h1>
      <p class="text-sand-500 mt-1">一文字ずつ隠された単語を当ててください。6回のチャンス</p>
    </div>
    
    <LazyGameWrapper game="hangman" lang="ja" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>