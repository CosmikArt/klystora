---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = 'スペリング・ビー — Klystora';
const description = '7文字で単語を作る。中央の文字を必ず使う. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">スペリング・ビー</h1>
      <p class="text-sand-500 mt-1">7文字で単語を作る。中央の文字を必ず使う</p>
    </div>
    
    <LazyGameWrapper game="spelling-bee" lang="ja" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>