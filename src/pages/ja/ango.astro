---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '暗号 — Klystora';
const description = '暗号を解読し、隠された名言を明らかにする. Free daily puzzle in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">暗号</h1>
      <p class="text-sand-500 mt-1">暗号を解読し、隠された名言を明らかにする</p>
    </div>
    
    <LazyGameWrapper game="cryptogram" lang="ja" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>