---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = '単語探し — Klystora';
const description = 'グリッドに隠されたすべての単語を見つける. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">単語探し</h1>
      <p class="text-sand-500 mt-1">グリッドに隠されたすべての単語を見つける</p>
    </div>
    
    <LazyGameWrapper game="word-search" lang="ja" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>