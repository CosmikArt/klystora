---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = 'Мульти-Вордл — Klystora';
const description = 'Решайте несколько досок Wordle одновременно. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-4xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">Мульти-Вордл</h1>
      <p class="text-sand-500 mt-1">Решайте несколько досок Wordle одновременно</p>
    </div>
    
    <LazyGameWrapper game="multi-wordle" lang="ru" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>