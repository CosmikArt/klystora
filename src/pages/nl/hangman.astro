---
import PwaInstallPrompt from '@/components/PwaInstallPrompt';
import Layout from '../../layouts/Layout.astro';
import LazyGameWrapper from '../../components/games/LazyGameWrapper';

const title = 'Hangman — Klystora';
const description = 'Guess the hidden word letter by letter. You have 6 lives. Free in 27 languages.';
const ogImage = '/og/default.jpg';
---

<Layout title={title} description={description} ogImage={ogImage}>
  <div class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">Hangman</h1>
      <p class="text-sand-500 mt-1">Guess the hidden word letter by letter. You have 6 lives.</p>
    </div>
    
    <LazyGameWrapper game="hangman" lang="nl" client:only="react" />
  </div>
  <PwaInstallPrompt client:only="react" />
</Layout>